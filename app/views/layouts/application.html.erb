<!DOCTYPE html>
<html>
  <head>
    <meta http-equiv="Content-Type" content="text/html; charset=UTF-8"/>
    <title><%= @pagetitle || (@page && @page.title) || 'Learn30' %></title>
    <meta name="title" content="Learn30 Test Application" />
    <meta name="description" content="Sample Rails Application" />
    <meta name="author" content="Paul Hahn" />

    <%= stylesheet_link_tag 'reset' %>
    <%= stylesheet_link_tag 'learningrails' %>
    <%= javascript_include_tag :defaults %>
    <%= csrf_meta_tag %>
  </head>
  <body>
  <%= render 'layouts/navigation' %>
  <br />

  <div id='pagewrapper'>
         <header>
			<p>Learning Rails Sample Application</p>
         <nav>
				<ul>
				  <li><%= link_to 'Home', view_page_path('home') %></li>
				  <li><%= link_to 'Resources',view_page_path('resources')  %></li>
			    <li><%= link_to 'About Us', view_page_path('about') %></li>
          <li><%= link_to 'Contact Us', view_page_path('contact') %></li>
				</ul>
         </nav>
		</header>
      <div id='content'>
      <%= render 'layouts/messages' %>
      <%= yield %>
		</div>
		<footer>
			<p>Learning Rails Sample Application</p>
      </footer>
  </body>
</html>
