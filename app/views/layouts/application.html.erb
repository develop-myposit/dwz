<!DOCTYPE html>
<html>
  <head>
    <meta http-equiv="Content-Type" content="text/html; charset=UTF-8"/>
    <title><%= @pagetitle %></title>
    <meta name="title" content="Dog Walk Zombie" />
    <meta name="description" content="Sample Rails Application" />
    <meta name="author" content="develop" />

    <%= google_webfonts_link_tag :montserrat %>
    <%= stylesheet_link_tag 'application' %>
    <%= javascript_include_tag "application" %>
    <%= csrf_meta_tag %>
  </head>
  <body>
  <%= render 'layouts/navigation' %><br />
  <div id='pagewrapper'>
         <header>
			<div class="banner"><%= link_to image_tag("dwz_header.jpg", alt: "Dog Walk Zombie"), root_path %></div>
         <nav>
				<ul>
              <% @tabs.each do |page| -%>
                <li <%= raw "id='current'" if @page && (@page == page || @page.parent == page )  %>>
                  <%= link_to page.navlabel, view_page_path(page.name) %>
               <% end -%>
               </li>
            </ul>
         </nav>
		</header>
      <div id='content'>
      <%= render 'layouts/messages' %>
      <%= yield %>
		</div>
		<footer>
			<p>Dog Walk Zombie</p>
      </footer>
  </body>
</html>
